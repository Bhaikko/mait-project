import React, { Component } from 'react';

import classes from './Profile.css';

class Profile extends Component {
    render () {
        return (
            <div>
                Someone else profile page
            </div>
        );
    }
}

export default Profile;